# Convex
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
CONVEX_SITE_URL=

# Webhook receiver URL (for generating webhook URLs)
# The Go receiver is proxied via go.webhooks.cc
NEXT_PUBLIC_WEBHOOK_URL=https://go.webhooks.cc

# Polar.sh
POLAR_ACCESS_TOKEN=
POLAR_WEBHOOK_SECRET=
POLAR_PRO_PRICE_ID=

# Go receiver (for calling Convex HTTP actions - use .site domain)
CONVEX_SITE_URL=

# SMTP (for transactional emails)
SMTP_HOST=mail.sauerdev.com
SMTP_PORT=465
SMTP_USER=postmaster@webhooks.cc
SMTP_PASS=
